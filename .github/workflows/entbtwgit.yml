name: entbtwgitSync

on:
  - push
  - delete

jobs:
  sync:
    runs-on: ubuntu-latest
    name: Git Repo Sync [entbtw git (^_^)]
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: wangchucheng/git-repo-sync@v0.1.0
      with:
        target-url: ${{ secrets.ENTBTWGIT_TARGET_URL }}
        target-username: ${{ secrets.ENTBTWGIT_TARGET_USERNAME }}
        target-token: ${{ secrets.ENTBTWGIT_TARGET_TOKEN }}